# نقشهٔ راه: دریافت خودکار خبر، بهبود کیفیت صدا، آمار فعالیت، تصویر همراه متن، ایده‌های بعدی

وضعیت: **طرح/برنامه — هنوز اجرا نشده.** این سند فقط برای ثبت تصمیم‌ها و طراحی فنیه، تا وقتی آماده بودیم پیاده‌سازی کنیم.

**به‌روزرسانی:** بخش «داشبورد Home» (بخش ۱) و «تصویر خودکار خبر» (بخش ۴) پیاده‌سازی و منتشر شدن (جزئیات در همون بخش‌ها). هم‌زمان دو فیچر خارج از محدودهٔ این سند هم به همراهشون ساخته شد: یک صفحهٔ **Library** جدا و جست‌وجوپذیر/مرتب‌شدنی برای کل متن‌های ذخیره‌شده (`ui/library/LibraryScreen.kt`)، و **بومی‌سازی کامل رابط کاربری** به فارسی/فرانسوی/انگلیسی + راست‌به‌چپ برای فارسی با فونت Vazirmatn (`data/LocalePrefs.kt`، منابع `values*/strings.xml`) — محتوای فرانسوی منبع (عنوان/خلاصهٔ خبر، متن مقاله) همیشه چپ‌به‌راست می‌مونه، حتی زیر رابط فارسی. انتخاب فونت دلخواه برای متن مقاله (جدا از اندازه‌ای که بخش ۷ داره) هنوز به‌عمد اضافه نشده.

## ۱. دریافت روزانهٔ خبر برای مطالعه

وضعیت: **پیاده‌سازی و منتشر شده — به شکل داشبورد Home، نه فقط یک دکمه.** RFI Français Facile و France Info به‌صورت هم‌زمان و مستقل از هم کش می‌شن (`news/NewsRepository.kt`، `data/HeadlineDao`) و در بخش «اخبار امروز» صفحهٔ Home نمایش داده می‌شن (`ui/home/HomeScreen.kt`)؛ باز کردن Home یا پیش‌نمایش یک خبر هیچ‌وقت متن کامل مقاله رو دانلود نمی‌کنه — فقط دکمهٔ «دریافت و خواندن» این کار رو انجام می‌ده (بدون WorkManager یا دانلود پس‌زمینه‌ای، دقیقاً طبق تصمیم اولیه). عکس هر خبر هم خودکار دانلود و ذخیره می‌شه (بخش ۴ رو ببینید). Le Monde به‌عنوان منبع سوم اضافه نشد؛ فقط همون دو منبع RFI/France Info که از قبل برای جست‌وجو موجود بودن.

تصمیم‌های گرفته‌شده با کاربر:
- **منبع:** هم متن‌های ساده‌شده برای زبان‌آموز (RFI – Journal en français facile) و هم اخبار واقعی/کامل (Le Monde یا France Info) — هر دو در دسترس، قابل انتخاب در تنظیمات.
- **زمان‌بندی:** بدون دانلود خودکار پس‌زمینه‌ای (بدون WorkManager). فقط یک دکمهٔ «دریافت خبر امروز» داخل اپ که وقتی کاربر خواست می‌زنه.

### طراحی فنی پیشنهادی

**تنظیمات:** یک فایل کوچک `NewsPrefs.kt` شبیه به الگوی `VocabPrefs.kt` که «منبع پیش‌فرض» رو در SharedPreferences نگه می‌داره؛ یک Dropdown ساده (در صفحهٔ لیست متن‌ها یا یک صفحهٔ تنظیمات جدید) برای انتخاب بین RFI Facile / Le Monde / France Info.

**دکمهٔ «دریافت خبر امروز»:** یک آیکون جدید در TopAppBar صفحهٔ لیست متن‌ها (TextsListScreen). با کلیک:
1. یک درخواست HTTP GET (روی `Dispatchers.IO`، با `HttpURLConnection` یا OkHttp) به فید RSS منبع انتخاب‌شده می‌زنه.
2. فید رو با `XmlPullParser` (استاندارد اندروید، بدون کتابخانهٔ اضافه) پارس می‌کنه و آخرین آیتم (اولین `<item>`) رو برمی‌داره.
3. عنوان + متن رو از `<description>` یا `<content:encoded>` می‌گیره (اگه موجود بود)، تگ‌های HTML رو پاک می‌کنه.
4. **جلوگیری از تکرار:** لینک/GUID خبرهای قبلاً واردشده رو نگه می‌داریم (SharedPreferences یا یه جدول کوچیک Room)؛ اگه آخرین خبر قبلاً وارد شده باشه، پیام «خبر جدیدی نیست» نشون داده می‌شه به‌جای ساختن نسخهٔ تکراری.
5. یک `TextDocument` جدید می‌سازه (از همون مسیر importی که الان برای Share/File استفاده می‌شه) با عنوان «[تاریخ] + عنوان خبر»، و مستقیم کاربر رو به صفحهٔ مطالعهٔ همون متن می‌بره (دقیقاً مثل رفتار فعلی share از یه اپ دیگه).
6. مدیریت خطا: اگه اینترنت نبود یا فید در دسترس نبود، یک Snackbar «دریافت خبر ممکن نشد» با دکمهٔ تلاش دوباره (مشابه الگوی retry موجود برای synthesizeChunk).
7. **تصویر خبر** (پیوند با بخش ۴): موقع پارس فید، اگه آیتم یه `<enclosure>`/`<media:content>`/`<img>` داشت، عکسش هم دانلود و همراه متن ذخیره می‌شه (اختیاری، نبودش مانع ساخت متن نیست).

### باقی‌مانده قبل از پیاده‌سازی
- آدرس دقیق فید RSS هر سه منبع باید با یک تست HTTP واقعی تأیید بشه (آدرس‌های RSS گاهی عوض می‌شن یا محدودیت دارن؛ الان فقط تأیید شد که هر سه منبع RSS دارن، نه آدرس دقیق‌شون).
- بررسی این‌که فید انتخابی متن کامل مقاله رو میده یا فقط خلاصه/لید. اگه فقط خلاصه بود (این برای Le Monde/France Info محتمل‌تره)، یا باید فید دیگه‌ای پیدا کرد یا یک مرحلهٔ استخراج از صفحهٔ HTML کامل خبر اضافه کرد (پیچیدگی بیشتر).

## ۲. بهبود کیفیت تولید صدا (TTS)

وضعیت فعلی: `edge-tts` (رایگان، صداهای Azure Neural مثل `fr-FR-DeniseNeural`) — این یکی از باکیفیت‌ترین گزینه‌های رایگان موجود در بازاره، پس نقطهٔ شروع خیلی خوبیه.

سه مسیر ممکن، به ترتیب اولویت پیشنهادی:

**الف) تنوع صدا (کم‌هزینه‌ترین و سریع‌ترین بهبود):** فیلد `voice` از قبل در دیتابیس هست ولی UI انتخابش وجود نداره. `edge-tts` چند صدای طبیعی فرانسوی دیگه هم داره: `fr-FR-HenriNeural` (مردونه)، `fr-FR-EloiseNeural`، و لهجه‌های `fr-CA` / `fr-BE` / `fr-CH`. یک Dropdown انتخاب صدا در تنظیمات هر متن اضافه کنیم — هم تنوع می‌ده هم تمرین شنیدن لهجه‌های مختلف که برای TCF مفیده.

**ب) گزینهٔ آفلاین/لوکال روی گوشی:** پروژهٔ متن‌باز [sherpa-onnx](https://github.com/k2-fsa/sherpa-onnx) (k2-fsa) دقیقاً برای اجرای مدل‌های TTS عصبی (از جمله صداهای [Piper](https://github.com/rhasspy/piper) با کیفیت خوب فرانسوی، و مدل‌های VITS/Kokoro) به‌صورت کاملاً آفلاین روی خود گوشی ساخته شده — نمونه‌های آماده مثل [VoxSherpa TTS](https://github.com/CodeBySonu95/VoxSherpa-TTS) دقیقاً همین کار رو روی اندروید انجام می‌دن. حجم مدل چند ده مگابایته و روی گوشی‌هایی مثل S24 FE سریع اجرا می‌شه؛ کیفیتش کمی از edge-tts پایین‌تره (چون edge-tts از سرورهای بزرگ مایکروسافت میاد) ولی کاملاً رایگان و بدون نیاز به اینترنته. پیشنهاد: این رو به‌عنوان یک **حالت آفلاین جایگزین** اضافه کنیم (وقتی اینترنت نیست، یا برای صرفه‌جویی در دیتا)، نه جایگزین همیشگی edge-tts، چون کیفیت فعلی برای تمرین تلفظ مهمه.

**ج) گزینه‌های دیگر (کمتر توصیه می‌شه):** Google Cloud TTS (سهمیهٔ رایگان محدود + نیاز به API key/صورتحساب)، ElevenLabs (کیفیت خیلی بالا ولی سهمیهٔ رایگان کم، برای متن‌های روزانهٔ طولانی کافی نیست).

جمع‌بندی: اول (الف) — ارزون و سریع؛ بعد در صورت نیاز واقعی (ب) به‌عنوان حالت آفلاین.

### اصلاحات باقی‌ماندهٔ انتخاب صدا

وضعیت: **در پلن — هنوز اجرا نشده.** انتخاب صدای آنلاین در UI پیاده‌سازی شده، اما صدای پیش‌فرض فعلی هنوز `fr-FR-DeniseNeural` است و labelهای `AVAILABLE_VOICES` به‌صورت متن فارسی داخل `tts/Voices.kt` hardcode شده‌اند؛ به همین دلیل حتی در رابط انگلیسی یا فرانسوی نیز نام و مشخصات صداها فارسی نمایش داده می‌شوند.

- **صدای پیش‌فرض:** صدای پیش‌فرض متن‌های جدید و fallback پخش لغات به `fr-FR-HenriNeural` (هانری، مرد، فرانسه) تغییر کند. این تغییر نباید انتخاب ذخیره‌شدهٔ متن‌های موجود را بازنویسی کند؛ متنی که کاربر قبلاً صدایش را انتخاب کرده، همان انتخاب را حفظ می‌کند.
- **بومی‌سازی نام صداها:** `VoiceOption` فقط شناسهٔ پایدار صدا و کلید/متادیتای لازم را نگه دارد و متن نمایشی از resourceهای `values`، `values-fr` و `values-fa` گرفته شود. نام شخص، جنسیت، کشور/لهجه و نشان «پیش‌فرض» باید در هر سه زبان انگلیسی، فرانسوی و فارسی درست نمایش داده شوند و هیچ label فارسی در کد Kotlin باقی نماند.
- **معیار پذیرش و تست:** در نصب/متن جدید، هانری به‌عنوان صدای انتخاب‌شده دیده و برای تولید صدا استفاده شود؛ تغییر زبان برنامه بدون restart اجباری، labelهای منوی صدا را به زبان رابط جدید نمایش دهد؛ و تست‌ها هم default/fallback هانری، حفظ صدای ذخیره‌شدهٔ متن‌های قدیمی، و کامل‌بودن ترجمهٔ همهٔ voice labelها را پوشش دهند.

## ۳. صفحهٔ آمار و فعالیت (Statistics)

هدف: یه صفحهٔ جدید (مثلاً یک آیکون کنار «لغات ذخیره‌شده» در نوار بالا) که خلاصه‌ای از فعالیت کاربر رو نشون بده.

### چیزهایی که نشون داده می‌شه
- مجموع زمان مطالعه/گوش‌دادن (کل، و هفتهٔ اخیر به تفکیک روز — یک نمودار میله‌ای ساده).
- تعداد کلمات مرورشده امروز/این هفته، و درصد «بلد بودم» در برابر «بلد نبودم» (دقت مرور).
- تعداد لغات در هر جعبهٔ لایتنر (۱ تا ۵) — یه نمودار میله‌ای کوچک نشون می‌ده کجای مسیر یادگیری هستید و چند تا لغت graduate شدن.
- روزهای پشت‌سرهم استفاده از اپ (streak)، شبیه دولینگو.
- تعداد متن‌های خونده‌شده/تکمیل‌شده، و مجموع کلمات ذخیره‌شده در طول زمان.
- (اختیاری، وابسته به آیتم «شمارش معکوس امتحان» در بخش ۵) روزهای باقی‌مانده تا تاریخ TCF.

### طراحی فنی
اپ الان فقط وضعیتِ *فعلی* رو نگه می‌داره (مثلاً `leitnerBox` و `lastReviewedAtMs` فقط آخرین مقدار رو دارن)، نه تاریخچه — پس برای نمودارهای روزانه به دو جدول کوچک جدید نیاز داریم:

1. **`ReviewLog`**: هر بار که «بلد بودم»/«بلد نبودم» زده می‌شه یک ردیف ثبت می‌کنیم (`entryId`, `timestampMs`, `knew: Boolean`, `boxBefore`, `boxAfter`). هم برای نمودار «تعداد مرور در روز» لازمه، هم برای محاسبهٔ درصد دقت.
2. **`ActivityLog`** (یا یه شمارندهٔ روزانهٔ ساده‌تر: `date` + `listeningMs`): از همون `positionTickerJob` که در `ReadingViewModel` هر ۱۵۰ میلی‌ثانیه پوزیشن رو چک می‌کنه، هر وقت `isPlaying` باشه چند میلی‌ثانیه به شمارندهٔ همون روز اضافه می‌کنیم.

`StatisticsScreen.kt` این دو جدول جدید + جدول `vocab` فعلی رو کوئری می‌کنه و خلاصه‌ها/نمودارهای ساده رو می‌سازه (با `Canvas` خود Compose، بدون نیاز به کتابخانهٔ نمودار اضافه).

این بخش کمی بزرگ‌تر از بقیهٔ فیچرهاست چون دو جدول تاریخچهٔ جدید و یک migration دیگه (شبیه همونی که برای لیست‌ها/لایتنر نوشتیم) لازم داره، ولی زیرساختش از قبل آماده‌ست.

## ۴. تصویر همراه متن (یادگیری چندحسی: صدا + تصویر + متن)

وضعیت: **بخش خودکار (خبر) پیاده‌سازی و منتشر شده؛ بخش افزودن دستی عکس هنوز ساخته نشده.** عکس هر خبر موقع پارس فید (از `media:content`/`media:thumbnail`/`enclosure`/`<img>` داخل توضیحات) استخراج می‌شه (`news/NewsFetcher.kt`)، و فقط وقتی کاربر «دریافت و خواندن» رو می‌زنه دانلود، به حداکثر عرض ۱۰۸۰ پیکسل با کیفیت JPEG ۸۲٪ فشرده، و در `filesDir/text_images/<id>.jpg` ذخیره می‌شه (`images/ArticleImageStore.kt`)؛ نبود عکس یا خطای دانلودش هیچ‌وقت جلوی ساخته‌شدن متن رو نمی‌گیره. حذف یک متن، عکس مالکیتی‌اش رو هم پاک می‌کنه (`content/ArticleImportRepository.kt`). گزینهٔ افزودن دستی عکس در `AddTextDialog` (برای متن‌های چسبانده‌شده/فایل) طراحی‌شده در این بخش هنوز اضافه نشده.

ایده: چون یادگیری از راه صدا + تصویر + متن با هم مؤثرتره، هر متن (خصوصاً خبر) بتونه یه عکس مرتبط داشته باشه — هم موقع افزودن دستی متن (مثل عنوان، بشه عکس هم انتخاب کرد)، هم به‌صورت خودکار برای خبرهای دریافتی از فید RSS (بخش ۱).

### وضعیت فعلی کد (بررسی شد)
صفحهٔ افزودن متن همین الان در `TextsListScreen.kt` هست: یک `AddTextDialog` ساده با فیلد «عنوان» و «متن فرانسوی»، و دکمهٔ «ذخیره و باز کردن». این دقیقاً همون جاییه که باید یک گزینهٔ افزودن عکس هم بهش اضافه بشه.

### طراحی فنی

**دیتابیس:** یک فیلد جدید `imagePath: String? = null` به `TextDocument` اضافه می‌شه (مسیر نسبی داخل فضای خصوصی اپ، مثلاً `text_images/<id>.jpg`). نیاز به یک migration جدید داره (v3 → v4، دقیقاً به همون سبک migration v2→v3 که برای لیست‌ها/لایتنر نوشتیم): `ALTER TABLE texts ADD COLUMN imagePath TEXT`.

**افزودن دستی عکس (در `AddTextDialog`):** زیر فیلد عنوان، یک ردیف کوچک اضافه می‌شه: اگه عکسی انتخاب نشده، یک دکمهٔ «افزودن عکس» (آیکون `Icons.Default.Image`)؛ اگه انتخاب شده، یک thumbnail کوچیک با دکمهٔ حذف کنارش. با `ActivityResultContracts.PickVisualMedia` (Photo Picker جدید اندروید — نیازی به مجوز READ_MEDIA_IMAGES نداره، از همون الگوی امنی که برای انتخاب فایل TXT/MD استفاده شده) عکس انتخاب می‌شه. چون `content://` URI بعد از بستن اپ ممکنه دیگه در دسترس نباشه، بلافاصله با `contentResolver.openInputStream` بایت‌هاش رو می‌خونیم و در `filesDir/text_images/` با یه نام یکتا کپی می‌کنیم؛ چون شناسهٔ متن تا وقتی insert نشده معلوم نیست، ترتیب کار اینه: اول insert بدون عکس (مثل الان)، بعد فوراً `update` با مسیر عکس (که بر پایهٔ همون id ساخته شده).

**نمایش:** یک کتابخونهٔ سبک لود عکس با کش لازمه — `Coil` (`io.coil-kt:coil-compose`)، گزینهٔ استاندارد و رایگان برای Compose، به `build.gradle` اضافه می‌شه.
- در `TextRow` (لیست متن‌ها): یک thumbnail کوچیک (مثلاً ۴۸dp، گوشه گرد) کنار عنوان، اگه `imagePath` موجود بود.
- در `ReadingScreen`: یک تصویر بزرگ‌تر (عرض کامل، ارتفاع ثابت مثلاً ۱۸۰dp، گوشه‌های گرد) بالای اولین بخش متن.

**تصویر خودکار برای خبر (وصل به بخش ۱):** موقع پارس فید RSS، اگه آیتم یکی از این‌ها رو داشت — `<enclosure url="..." type="image/...">`، `<media:content url="...">`، یا یه تگ `<img src="...">` داخل `<description>` — همون URL دانلود می‌شه (یه GET ساده روی `Dispatchers.IO`) و دقیقاً مثل حالت دستی در `filesDir/text_images/` ذخیره می‌شه. نبود عکس در فید مشکلی ایجاد نمی‌کنه، متن بدون عکس مثل الان اضافه می‌شه.

### نکات جانبی
- **حجم فایل:** عکس‌های خبری معمولاً چند صد کیلوبایت تا ۱-۲ مگابایتن. برای جلوگیری از پر شدن فضای گوشی، قبل از ذخیره با `Bitmap.compress` کمی فشرده/تغییر‌اندازه می‌دیم (مثلاً حداکثر عرض ۱۰۸۰ پیکسل، کیفیت JPEG حدود ۸۰٪).
- **حذف تمیز:** الان `TextsListViewModel.delete()` فقط ردیف دیتابیس رو حذف می‌کنه؛ باید قبلش فایل `imagePath` (اگه غیر null بود) رو هم از حافظهٔ داخلی پاک کنیم تا فایل یتیم نمونه.

این فیچر با فیچر «دریافت خبر» (بخش ۱) و «آمار» (بخش ۳) هم‌پوشانی مفیدی داره — پیشنهاد می‌کنم اگه/وقتی خواستیم شروع کنیم، اول بخش دستی (افزودن عکس در `AddTextDialog` + نمایش) رو بسازیم چون سرراست‌تره، و بعد بخش خودکار (استخراج عکس از فید خبر) رو روش سوار کنیم.

## ۵. ایده‌های دیگر — با طراحی مختصر

- **هایلایت سراسری لغات ذخیره‌شده در متن‌ها**: هر واژه‌ای که به فهرست لغات اضافه شده، در همهٔ متن‌ها (نه فقط متن مبدأ) با یک هایلایت ملایم مشخص بشه تا کاربر بدونه قبلاً ذخیره‌اش کرده و دوباره برای افزودنش تلاش نکنه. لمس واژهٔ هایلایت‌شده وضعیت «از قبل در لغات شماست» رو نشون بده و راه مشاهده یا حذفش رو فراهم کنه. تطبیق در نسخهٔ اول با نرمال‌سازی حروف بزرگ/کوچک و حذف علائم نگارشی انجام بشه، اما شکل‌های صرفی متفاوت مثل `mange` و `manger` جدا بمونن؛ یکسان‌سازی براساس lemma می‌تونه بعداً به‌عنوان قابلیت مستقل اضافه بشه. رنگ باید به‌اندازهٔ کافی ملایم باشه که خوانایی متن و هایلایت فعال پخش صدا رو مختل نکنه، و بهتره وضعیت «ذخیره‌شده» و «یادگرفته‌شده» نشانه‌های بصری متفاوت داشته باشن.
- **کوییز درک مطلب خودکار**: بعد از هر متن چند سؤال چهارگزینه‌ای دربارهٔ محتواش نشون بده. چالش اصلی: تولید خودکار سؤال خوب واقعاً نیاز به یه مدل زبانی داره (نه فقط قواعد ساده)، یعنی یا صدا زدن یه API خارجی (هزینه‌بر، نیاز به کلید API)، یا از پیش ساختن بانک سؤال دستی برای متن‌های رایج. سنگین‌ترین آیتم این لیست.
- **برچسب سطح دشواری متن (A2/B1/B2/C1)**: بر اساس میانگین طول جمله + یه دیکشنری فراوانی لغات فرانسوی (مثل Lexique383، رایگان و قابل دانلود). کاملاً محلی، بدون نیاز به API، اجرایی نسبتاً ساده.
- **حالت سایه‌خوانی (shadowing)**: پخش یه جمله، مکث، دکمهٔ ضبط با `SpeechRecognizer` آفلاین اندروید (fr-FR)، و نمایش متن تشخیص‌داده‌شده کنار جملهٔ اصلی برای مقایسه. نیاز به مجوز میکروفون و پکیج زبان فرانسوی روی گوشی. برای تمرین بخش گفتاری TCF خیلی مفیده.
- **شمارش معکوس امتحان**: یه فیلد «تاریخ امتحان TCF» در تنظیمات؛ در صفحهٔ آمار (بخش ۳) نشون داده می‌شه.
- **دکمهٔ صرف فعل**: یه `IconButton` کوچک کنار دکمهٔ «باز کردن در مرورگر» در `DictionarySheet`، که به‌جای صفحهٔ `/fren/` به صفحهٔ `/conjugation/frconjugaison/` وردرفرنس می‌ره. تغییر خیلی کوچک و ارزون.
- **دانلود چند روز خبر با هم**: وقتی رو Wi-Fi هستید، دکمهٔ «دریافت خبر امروز» می‌تونه گزینهٔ «دریافت N خبر اخیر» هم داشته باشه (اگه فید اون‌قدر آیتم قدیمی نگه داره) — برای مطالعهٔ آفلاین بعداً.
- **ویجت صفحهٔ اصلی اندروید**: تعداد لغات آمادهٔ مرور امروز رو نشون بده و مستقیم به صفحهٔ مرور ببره (شبیه ویجت دولینگو). نیاز به `AppWidgetProvider` جدا و طراحی layout XML جدا — پیچیدگی متوسط، جدا از بقیهٔ اپ.
- **خروجی Anki/CSV**: تبدیل جدول `vocab` به فایل `.csv` (ساده) یا فرمت `.apkg` اَنکی (نیاز به کتابخانهٔ genanki یا نوشتن فرمت SQLite خاص انکی — کمی پیچیده‌تر از CSV).

## ۶. منابع بیشتر برای مطالعه (فراتر از خبر RSS)

وضعیت: **طرح/برنامه — Vikidia، RFI Facile و France Info (با معماری یکپارچهٔ `ContentSource`) پیاده‌سازی و منتشر شده؛ بقیهٔ منابع هنوز طراحی نشده‌اند.**

راهبرد این بخش اینه که منابع دارای متن تمیز، API یا کاتالوگ رسمی و وضعیت مجوز روشن در اولویت باشن. سه منبع اصلی عبارت‌اند از **Vikidia برای مطالب آموزشی ساده، Wikisource برای ادبیات و Project Gutenberg برای کتاب‌های قابل دانلود**. منابع خبری فعلی باقی می‌مونن، اما RSS فقط باید برای کشف مطلب استفاده بشه؛ ذخیرهٔ متن برای مطالعه وقتی انجام می‌شه که متن کامل و مجوز مناسب در دسترس باشه.

### پس‌زمینه: مشکل فعلی خبر RSS

بعد از پیاده‌سازی بخش ۱، مشخص شد با فچ زنده هر دو فید مشکلی وجود داره: `<description>`/`<content:encoded>` هر دو منبع فقط یکی-دو جملهٔ خلاصه/تبلیغاتی می‌دن، نه متن کامل:
- **France Info** (`titres.rss`): `<description>` یک یا دو جملهٔ خلاصه‌ست؛ فید اصلاً `content:encoded` نداره. متن کامل فقط در صفحهٔ خبر (لینک `<link>`) هست.
- **RFI Facile**: `<description>` و `content:encoded` هر دو یک جملهٔ تبلیغاتی‌ان («... Retrouvez votre épisode avec la transcription synchronisée ... : https://rfi.my/...») که به صفحهٔ رونویسی لینک می‌دن، نه خود رونویسی.

در نتیجه کاربر به‌جای مقاله فقط چند کلمه/جمله می‌بینه. راه‌حل نهایی (بخش «طراحی فنی: جست‌وجوی یکپارچهٔ منابع» پایین‌تر رو ببینید): گرفتن چند آیتم اخیر RSS برای *کشف/جست‌وجوی* خبر، و بعد fetch متن کامل از صفحهٔ `<link>` همون خبر با jsoup.

### اولویت منابع و تفکیک به زیرپروژه‌ها

هر اتصال یک زیرپروژهٔ مستقل با بررسی مجوز، طراحی و پیاده‌سازی جداست. ترتیب پیشنهادی توسعه:

| اولویت | منبع/زیرسیستم | نوع محتوا و روش اتصال | تصمیم و وضعیت |
|---:|---|---|---|
| ۱ | **Vikidia** | مقاله‌های سادهٔ علمی، تاریخی، جغرافیایی و فرهنگی؛ MediaWiki API برای جست‌وجو و دریافت متن | بهترین شروع برای A2–B1؛ **طراحی و پلن اجرایی آماده است** |
| ۲ | **RFI Français Facile** | RSS برای کشف اپیزود، سپس دریافت transcription کامل از صفحهٔ اپیزود | بهترین منبع خبر آموزشی؛ **پیاده‌سازی و منتشر شده** (بخش «جست‌وجوی یکپارچه») |
| ۳ | **Wikisource فرانسوی** | ادبیات و متون کلاسیک؛ MediaWiki API، با انتخاب فصل/داستان به‌جای ورود یک‌بارهٔ کتاب بلند | مناسب B1 به بالا؛ **طراحی فنی آماده است** (پایین‌تر) |
| ۴ | **Project Gutenberg** | کتاب و داستان فرانسوی مالکیت عمومی؛ کاتالوگ OPDS و دانلود TXT/EPUB | اتصال باید قابل تعویض باشه چون OPDS XML ممکنه در ۲۰۲۷ کنار گذاشته بشه؛ طراحی نشده |
| ۵ | **The Conversation France** | مقاله‌های علمی و تحلیلی B2–C1؛ RSS برای فهرست و دریافت مطلب انتخاب‌شده | فقط دانلود موردی با انتساب کامل؛ برداشت انبوه انجام نشه؛ طراحی نشده |
| ۶ | **Gallica** | مجلات، روزنامه‌ها و کتاب‌های تاریخی؛ API رسمی و OCR | به‌دلیل کیفیت متغیر OCR و پیچیدگی بیشتر، بعد از منابع بالا؛ طراحی نشده |
| — | **France Info** | خبر عمومی؛ RSS برای کشف و صفحهٔ خبر برای متن کامل | **پیاده‌سازی و منتشر شده** (بخش «جست‌وجوی یکپارچه») |
| — | **TV5MONDE Apprendre** | تمرین، گرامر، ویدئو و متن آموزشی | فقط بازکردن صفحه یا ورود متن با Share؛ استخراج و ذخیرهٔ خودکار در برنامه نیست |

`Wikinews` در برنامهٔ توسعه قرار نمی‌گیره، چون نسخهٔ فرانسوی اعلام کرده از ۴ مهٔ ۲۰۲۶ به‌روزرسانی نمی‌شه.

### سیاست دریافت، مجوز و انتساب

- «رایگان برای خواندن» به معنی «مجاز برای استخراج و ذخیرهٔ خودکار» نیست. دریافت خودکار متن کامل فقط برای منابع با مجوز روشن یا اجازهٔ بازنشر مناسب فعال می‌شه.
- برای Vikidia مقدارهای `sourceUrl`، `sourceName` و `license` همیشه ذخیره می‌شن. برای Wikisource، Gutenberg و منابع بعدی نیز اطلاعات کتاب‌شناختی و مجوز متناسب با منبع ثبت می‌شه.
- The Conversation فقط به درخواست کاربر و برای یک مقالهٔ مشخص دریافت می‌شه؛ نام نویسنده، مؤسسه، لینک و مجوز همراه متن ذخیره می‌شن و متن محتوایی تغییر نمی‌کنه.
- TV5MONDE و سایت‌های خبری فاقد مجوز روشن از طریق مرورگر یا Share استفاده می‌شن، نه با کپی خودکار متن کامل.
- مدل مشترک انتساب برای منابع خارجی شامل `sourceUrl`، `sourceName`، `author`، `license`، `publishedAt` و شناسهٔ اختصاصی منبع خواهد بود. شناسهٔ منبع هنگام طراحی اتصال‌های بعدی به مدل داده اضافه می‌شه.

### تجربهٔ هدف: «پیدا کردن مطلب»

در مسیر نهایی، دکمهٔ محدود «دریافت خبر امروز» به تجربهٔ عمومی‌تر «پیدا کردن مطلب» توسعه پیدا می‌کنه:

1. کاربر نوع محتوا رو انتخاب می‌کنه: خبر، آموزشی، داستان یا کتاب.
2. موضوع، سطح تقریبی و طول مطلوب رو تعیین می‌کنه.
3. برنامه تا ۱۰ نتیجه با عنوان، منبع، طول و سطح احتمالی نمایش می‌ده.
4. فقط مواردی که کاربر انتخاب کرده دانلود می‌شن.
5. متن و اطلاعات منبع/مجوز با هم ذخیره می‌شن.

فاز اول Vikidia عمداً کوچک‌تره: جست‌وجوی موضوعی، نمایش حداکثر ۱۰ نتیجه و ورود تک‌مطلب، بدون فیلتر سطح/طول و بدون انتخاب چندتایی. قابلیت‌های عمومی بالا بعد از اثبات این مسیر به‌صورت مرحله‌ای اضافه می‌شن.

پلن اجرایی فاز اول: [`docs/superpowers/plans/2026-09-16-vikidia-content-discovery.md`](docs/superpowers/plans/2026-09-16-vikidia-content-discovery.md).

### طراحی فنی: Vikidia

**API:** بدون کلید/ثبت‌نام، MediaWiki API استاندارد روی `fr.vikidia.org`. مجوز محتوا: **CC BY-SA 3.0** (تأیید شده از `action=query&meta=siteinfo&siprop=rightsinfo`).

- جست‌وجو: `GET /w/api.php?action=query&list=search&srsearch=<query>&srlimit=10&srprop=snippet|wordcount&format=json` — یک درخواست، همون‌جا عنوان + خلاصه (با تگ `<span class="searchmatch">`) + تعداد کلمه (به‌عنوان تخمین طول) رو می‌ده؛ نیازی به fetch جدا برای هر نتیجهٔ لیست نیست.
- گرفتن مقالهٔ کامل: `GET /w/api.php?action=query&prop=extracts|revisions&explaintext=1&rvprop=timestamp&pageids=<id>&format=json` — متن plain-text تمیز (خود API ویکی‌تکست/HTML رو پاک می‌کنه، برخلاف RSS نیازی به strip دستی نیست) + تاریخ آخرین ویرایش.

هر دو با همون الگوی `HttpURLConnection` GET بدون کتابخونهٔ اضافه که `NewsFetcher` استفاده می‌کنه (User-Agent، timeout ۱۵ ثانیه)، و پارس JSON با `org.json` استاندارد اندروید. کد در `VikidiaClient.kt` (پکیج جدید، مثلاً `com.ziaee.frenchreader.vikidia`). تابع `stripHtml` از `NewsFetcher.kt` به `util/HtmlUtil.kt` مشترک منتقل می‌شه تا هم `NewsFetcher` هم `VikidiaClient` (برای پاک کردن تگ `searchmatch` از خلاصه‌ها) ازش استفاده کنن.

**مدل داده:** پنج فیلد nullable جدید به `TextDocument` (`Entities.kt`): `sourceUrl: String?`, `sourceName: String?`, `author: String?`, `license: String?`, `publishedAt: Long?` (epoch ms). یک `MIGRATION_3_4` دقیقاً به سبک `MIGRATION_2_3` موجود در `AppDatabase.kt` (پنج `ALTER TABLE texts ADD COLUMN ... TEXT`)، و `@Database(version = 4, ...)` + `.addMigrations(MIGRATION_2_3, MIGRATION_3_4)`.
برای Vikidia: `sourceName = "Vikidia"`, `author = null` (ویکی مشارکتیه، بدون نویسندهٔ واحد)، `license = "CC BY-SA 3.0"`, `sourceUrl = "https://fr.vikidia.org/wiki/<عنوان URL-encode شده>"`, `publishedAt` از `revisions[0].timestamp` (ISO 8601 → epoch ms).
متن‌های خبری فعلی (RSS) و متن‌های دستی/فایلی این فیلدها رو `null` می‌ذارن — بدون تغییر رفتار موجود.

**UI (`TextsListScreen.kt`):** یک آیکون **جدید** («پیدا کردن مطلب»، مثلاً `Icons.Default.Search`) کنار آیکون فعلی خبر (`Newspaper`) در `TopAppBar` اضافه می‌شه؛ آیکون/Dropdown فعلی RFI/France Info دست‌نخورده می‌مونه، کاملاً جدا. با لمس، یک `ModalBottomSheet` باز می‌شه (هم‌سبک `DictionarySheet` موجود):
1. بالای sheet: یک `OutlinedTextField` برای موضوع/کلیدواژه + دکمهٔ جست‌وجو (submit روی IME action یا دکمه، بدون live-search).
2. پایین: لیست تا ۱۰ نتیجه (عنوان، خلاصه، تعداد کلمه به‌عنوان تخمین طول تقریبی).
3. لمس یک نتیجه → `VikidiaClient.fetchArticle(pageId)` (با spinner روی همون آیتم) → ساخت `TextDocument` → باز شدن صفحهٔ مطالعه، دقیقاً مثل رفتار فعلی RFI/France Info در `TextsListViewModel.fetchNews`.

فیلتر سطح/طول و انتخاب چندتایی (که در طراحی اولیهٔ کاربر مطرح شده بود) در این مرحله **در scope نیست** — تک‌انتخابی، بدون فیلتر سطح (تخمین سطح دشواری نیاز به منطق جدا داره، بخش ۵ رو ببینید).

**نمایش منبع در صفحهٔ مطالعه (`ReadingScreen.kt`):** یک آیکون info کوچک به `TopAppBar` اضافه می‌شه، **فقط وقتی `sourceUrl != null`** (برای متن‌های فعلی نامرئی می‌مونه). لمسش یک `ModalBottomSheet` کوچیک باز می‌کنه: نام منبع، مجوز، تاریخ انتشار، دکمهٔ «باز کردن منبع در مرورگر» (`Intent.ACTION_VIEW`، هم‌سبک دکمهٔ مشابه در `DictionarySheet`).

**خطاها:** جست‌وجوی ناموفق (بدون اینترنت/خطای API) → Snackbar با پیام خطا. بدون نتیجه → پیام متنی داخل sheet. دانلود مقالهٔ انتخابی ناموفق → Snackbar، ولی sheet باز می‌مونه تا کاربر نتیجهٔ دیگه‌ای امتحان کنه (sheet بسته نمی‌شه).

**تست:** پروژه فعلاً تست خودکار نداره (نه برای `NewsFetcher` هم)، پس تأیید با اجرای واقعی روی دستگاه/شبیه‌ساز (چند جست‌وجوی موضوع، بررسی متن دریافتی و attribution نمایش‌داده‌شده).

**اصلاح بعد از انتشار:** بعد از استفادهٔ واقعی مشخص شد `extract` ویکیدیا (حالت `explaintext`) نحو تیتر ویکی‌متن (`== عنوان ==`) رو به‌صورت متن خام نگه می‌داره، که TTS عیناً می‌خونتش. راه‌حل: تبدیل این نحو به تیتر Markdown استاندارد (`N` تا `=` ← `N` تا `#`) در `VikidiaClient.parseArticle` (تابع `convertWikiHeadings`) — چون اپ از قبل `BlockType.HEADER` رو در `MarkdownParser.kt` کامل پشتیبانی می‌کنه (فونت جدا، TTS درست می‌خونه)، فقط کافی بود نحو درست بشه؛ جای دیگه‌ای نیاز به تغییر نداشت.

### طراحی فنی: جست‌وجوی یکپارچهٔ منابع (Vikidia + RFI + France Info)

وضعیت: **پیاده‌سازی و منتشر شده.** (شیت «پیدا کردن مطلب» حالا Vikidia + RFI Facile + France Info رو موازی جست‌وجو می‌کنه؛ آیکون/Dropdown قدیمی خبر حذف شده.)

بعد از تجربهٔ واقعی Vikidia، مشخص شد نگه‌داشتن آیکون/Dropdown قدیمی RFI/France Info کنار شیت جست‌وجوی Vikidia هم گیج‌کننده‌ست و هم بی‌فایده — چون خبر RSS فعلی هنوز همون مشکل قدیمی رو داره (فقط خلاصه، نه متن کامل؛ بخش «پس‌زمینه» بالا رو ببینید). به‌جای اصلاح مجزای RFI/France Info، این بار طراحی **یکپارچه** انجام می‌شه: یک انتزاع مشترک (`ContentSource`) که Vikidia و RSSهای خبری هر دو پیاده‌ش می‌کنن، و شیت «پیدا کردن مطلب» همه رو با هم جست‌وجو می‌کنه.

**انتزاع مشترک (پکیج جدید `com.ziaee.frenchreader.content`):**

```kotlin
interface ContentSource {
    val id: String
    val label: String
    suspend fun search(query: String, limit: Int = 10): List<ContentResult>
    suspend fun fetchArticle(result: ContentResult): ContentArticle?
}
data class ContentResult(val sourceId: String, val sourceLabel: String, val title: String, val snippet: String, val lengthHint: String, val ref: String)
data class ContentArticle(val title: String, val text: String, val sourceUrl: String, val sourceName: String, val author: String?, val license: String?, val publishedAtMs: Long?)
```

**`VikidiaContentSource`:** آداپتور نازک روی `VikidiaClient` موجود (بدون تغییر در خود `VikidiaClient`).

**`RfiFacileContentSource` / `FranceInfoContentSource`** (پکیج فرعی جدید `content/news`): برخلاف Vikidia، فید RSS این دو منبع اصلاً جست‌وجوی کلیدواژه‌ای نداره — فقط «آخرین آیتم» رو می‌ده. راه‌حل:
- `search(query)`: به‌جای گرفتن فقط اولین `<item>` (رفتار فعلی `NewsFetcher`)، حدود ۲۰-۳۰ آیتم اخیر فید گرفته می‌شه و روی عنوان+توضیح با کلیدواژه فیلتر می‌شه (کلاینت‌ساید، بدون API جست‌وجوی سمت سرور چون RSS چنین چیزی نداره). پارسر تک‌آیتمی فعلی `NewsFetcher` باید به یک پارسر چندآیتمی تعمیم پیدا کنه که هر دو منبع (RSS خبر فعلی و منابع جدید) ازش استفاده کنن.
- `fetchArticle(result)`: صفحهٔ وب واقعی خبر/اپیزود (لینک آیتم RSS) گرفته می‌شه و متن کامل با **jsoup** استخراج می‌شه (کتابخونهٔ جدید، `org.jsoup:jsoup`). سلکتورها با فچ زنده تأیید شدن:
  - **France Info:** `div.c-body` — قبل از استخراج متن، بلوک‌های `.media-embed` (کارت‌های «À lire aussi») حذف می‌شن، بعد متن `<p>`/`<h2>` گرفته می‌شه.
  - **RFI Facile:** `div.m-transcription__content p` — دقیقاً رونویسی کامل همگام‌شده‌ای که در صفحهٔ اپیزود هست (نیازی به PDF نیست)؛ ساختار تمیزه، نیاز به حذف بلوک اضافه نداره.

**UI (`TextsListScreen.kt`):** آیکون/Dropdown قدیمی خبر (`Newspaper`) **حذف می‌شه**. شیت «پیدا کردن مطلب» فعلی (که برای Vikidia ساخته شده) هر سه منبع رو موازی جست‌وجو می‌کنه و نتیجه‌ها رو با هم در یک لیست نشون می‌ده؛ هر ردیف نام منبعش رو هم نشون می‌ده (برای تمایز Vikidia از خبر). `TextsListViewModel.fetchNews`/`NewsFetchUiState`/دراپ‌داون مرتبط حذف می‌شن.

**حذف جلوگیری از تکرار:** `NewsPrefs` (نگه‌داری آخرین guid وارد‌شده برای جلوگیری از تکرار) حذف می‌شه — چون در مدل جدید (مثل Vikidia) کاربر آگاهانه هر نتیجه رو انتخاب می‌کنه، نیازی به dedup خودکار نیست.

**خطاها:** همون الگوی Vikidia — خطای جست‌وجو/دانلود هر منبع جدا مدیریت می‌شه؛ اگه یک منبع خطا بده، نتیجه‌های منابع دیگه همچنان نشون داده می‌شن (fail کردن یک منبع نباید کل جست‌وجو رو خراب کنه).

**ریسک شناخته‌شده:** سلکتورهای jsoup به ساختار HTML دو سایت خبری وابسته‌ن و با ریدیزاین سایت ممکنه خراب بشن — همون ریسک هر ابزار scraping. قابل قبول برای این مرحله.

**اصلاح بعد از بررسی نهایی (final whole-branch review):** دو باگ Critical پیدا و رفع شد که باعث می‌شد دریافت خبر همیشه fail بشه: (۱) `fetchArticle` فراخوانی شبکه رو روی dispatcher اصلی (نه IO) انجام می‌داد → `NetworkOnMainThreadException`؛ (۲) لینک آیتم‌های RSS Facile RFI به‌صورت `http://` بودن ولی اپ ترافیک cleartext رو مسدود می‌کنه → خطای دسترسی مسدود. هر دو رفع شدن (`withContext(Dispatchers.IO)` + ارتقای خودکار scheme به https). همچنین `publishedAtMs` خبرها که قبلاً همیشه null می‌شد، حالا درست منتقل می‌شه.

**باقی‌مانده برای بعد (غیر مسدودکننده):**
- ~~طبیعی‌کردن جست‌وجوی صفحهٔ Home~~ — **پیاده‌سازی و تست شده.** لمس کادر جست‌وجوی Home مستقیم `FindArticleSheet` را باز می‌کند و همان‌جا فیلد واقعی خودکار focus می‌گیرد و کیبورد نمایش داده می‌شود (بدون لمس دوم) — چون `LaunchedEffect` درخواست focus باید داخل محتوای `ModalBottomSheet` باشد (زیرکامپوزیشن جدا)، نه بالای تابع، وگرنه با خطای «FocusRequester is not initialized» روی دستگاه واقعی fail می‌شود (این با اجرای واقعی تست پیدا و رفع شد). فیلد `ImeAction.Search` دارد و دکمهٔ Search کیبورد دقیقاً همان تابع `submit()` آیکون ذره‌بین را صدا می‌زند؛ `submit()` برای query خالی یا وقتی `searchState` از قبل `Searching` است کاری نمی‌کند. آیکون ذره‌بین همچنان به‌عنوان راه جایگزین باقی مانده. پوشش تست: `FindArticleSheetTest.kt` (۵ تست — focus خودکار، submit با IME، submit با آیکون، رد ورودی خالی، رد resubmit حین جست‌وجوی در حال اجرا)، روی دستگاه واقعی اجرا و سبز شد.
- ورودی «آخرین‌ها بدون کلیدواژه»: چون `searchContent` روی query خالی زودتر برمی‌گرده، دیگه راهی برای دیدن آخرین خبرها بدون تایپ کلیدواژه نیست (رفتار قدیمی دکمهٔ Newspaper). می‌شه بعداً query خالی رو هم مجاز کرد و به‌عنوان «آخرین‌ها» تفسیر کرد.
- پوشش تست برای خود `ContentSource` (متدهای `search`/`fetchArticle`) و منطق merge با تحمل خطای `searchContent` — فعلاً فقط توابع خالص داخلی تست شدن؛ نیاز به یک `ContentSource` فیک + `runBlocking` که هنوز زیرساختش در پروژه نیست.
- سه نسخهٔ تکراری از تابع کمکی `httpGet`/`httpGetHtml` (در `VikidiaClient`، `NewsFetcher`، `NewsContentSource`) — می‌شه یکی کرد؛ صرفاً کیفیت کد، نه باگ.
- نتیجه‌های منابع مختلف در لیست جست‌وجو به‌ترتیب منبع پشت‌سرهم میان (Vikidia قبل از خبر)، نه به‌ترتیب ارتباط/تاریخ؛ و duplicate نمی‌شن.

### طراحی فنی: Wikisource فرانسوی

وضعیت: **طرح — آمادهٔ پیاده‌سازی.** (پیاده‌سازی نشده؛ یافته‌های زیر با فراخوانی واقعی API در ۲۰۲۶-۰۹-۱۷ تأیید شدن.)

**API:** بدون کلید/ثبت‌نام، همون MediaWiki API استاندارد روی `fr.wikisource.org` که Vikidia ازش استفاده می‌کنه. مجوز محتوا: **CC BY-SA 4.0** (تأیید شده از `action=query&meta=siteinfo&siprop=rightsinfo`) — این مجوز مال لایهٔ رونویسی/قالب‌بندی Wikisource‌ه، نه لزوماً اثر ادبی مبدأ (که معمولاً خودش هم به‌خاطر گذشت زمان از حق‌تکثیر، مالکیت عمومیه؛ ذخیرهٔ `license = "CC BY-SA 4.0"` برای انتساب همینه که در متن `TextDocument` نگه می‌داریم، دقیقاً هم‌سبک Vikidia).

**تفاوت کلیدی با Vikidia (تأیید شده با فراخوانی زنده، نه فرض):**
- **API استخراج متن (`prop=extracts`) روی Wikisource غیرفعاله** — فراخوانی مستقیمش روی یک صفحهٔ واقعی مقدار `"extract":""` خالی برمی‌گردونه. پس روش `explaintext` که برای Vikidia کار می‌کرد، اینجا اصلاً جواب نمی‌ده.
- خیلی از صفحه‌های Wikisource (خصوصاً آثار «Textes validés» که از روی اسکن تأیید شدن) اصلاً متن رو در wikitext نگه نمی‌دارن؛ به‌جاش یه تگ transclusion مثل `<pages index="Maupassant - Boule de suif.djvu" from=7 to=61 header=1/>` دارن که متن رو از namespace جدا (`Page:`) سر هم می‌کنه. یعنی خوندن مستقیم wikitext (`action=query&prop=revisions`) برای این صفحه‌ها عملاً چیزی به‌جز همین تگ برنمی‌گردونه.
- راه‌حل یکسان برای هر دو حالت (متن مستقیم در wikitext، یا transclusion از `Page:`): **`action=parse&page=<title>&prop=text&format=json`** — این HTML کامل رندرشده رو برمی‌گردونه، صرف‌نظر از این‌که متن مبدأ کجا ذخیره شده؛ روی صفحهٔ واقعی «Boule de suif (recueil)/Boule de Suif» تست شد و ~۱۰۰ کیلوبایت HTML کامل داستان برگردوند. پس `WikisourceClient.fetchArticle` باید HTML رو با **jsoup** پردازش کنه، نه JSON متن ساده.

**جست‌وجو:** همون الگوی Vikidia — `GET /w/api.php?action=query&list=search&srsearch=<query>&srlimit=10&srprop=snippet|wordcount&format=json`، یک درخواست، بدون fetch جدا برای هر نتیجه.

**پاکسازی HTML برای `fetchArticle` (سلکتورهای jsoup، تأیید شده روی نمونهٔ واقعی):**
- حذف `.ws-noexport` (اعلان «برای نسخه‌های دیگهٔ این متن ببینید...» + بلوک‌های پانویس/اعتبار مربوط به ویرایش صفحه، نه خود اثر).
- حذف `.headertemplate` (بلوک عنوان/نویسنده/منبع بالای صفحه — تکراریه چون همین اطلاعات رو خودمون در `sourceName`/`author`/`sourceUrl` ذخیره می‌کنیم).
- حذف `.pagenum`/`.ws-pagenum` (نشانگرهای شمارهٔ صفحهٔ اسکن که وسط پاراگراف‌ها درج می‌شن، وگرنه TTS عین یه عدد بی‌ربط می‌خوندشون).
- حذف هر `<table>` باقی‌مونده (معمولاً باقیماندهٔ صفحه‌های فهرست/edition-list، نه خود متن).
- نگه‌داشتن `<p>` و `div.poem` (با `<br>` → خط جدید، برای شعر) به‌عنوان پاراگراف‌های معمولی.
- تبدیل `<h1>`–`<h6>` واقعی HTML (نه نحو wikitext، چون ورودی همین‌جا رندرشده‌ست) به نحو Markdown اپ (`#`..`######`) — دقیقاً همون‌کاری که `MarkdownParser.kt`/`BlockType.HEADER` از قبل پشتیبانی می‌کنه (مشابه اصلاح بعد از انتشار Vikidia، ولی این‌جا از سطح HTML ساده‌تره چون دیگه لازم نیست نحو `==عنوان==` ویکی‌متن رو دستی تشخیص بدیم).

**ریسک شناخته‌شده (پذیرفتنی، هم‌سبک ریسک RSS):** نتیجه‌های جست‌وجو مخلوطی از صفحه‌های واقعی داستان/فصل، صفحه‌های فهرست/دیزامبیگ (مثلاً خود عنوان «Boule de suif» یه صفحهٔ فهرست نسخه‌هاست، نه خود داستان — نسخهٔ واقعی زیر عنوان جداییه)، و صفحه‌های بیوگرافی نویسنده‌ست. در v1 فیلتر سمت سرور انجام نمی‌شه؛ همون `wordcount` که از `srprop` می‌گیریم (که از قبل به‌عنوان «تخمین طول» در UI نشون داده می‌شه) تنها راهنمای کاربر برای رد کردن نتیجه‌های خیلی کوتاه/فهرست‌مانند می‌مونه.

**محدودهٔ granularity:** هر نتیجهٔ جست‌وجو دقیقاً یک صفحهٔ Wikisource (یک داستان کوتاه/یک فصل/یک شعر) رو نشون می‌ده، نه کل کتاب. برای داستان‌های کوتاه (Conte/Nouvelle) این معمولاً یعنی کل اثر در یک صفحه‌ست و مشکلی نیست؛ برای یه رمان بلند مثل «Les Misérables» کاربر باید هر فصل رو جدا جدا جست‌وجو/دانلود کنه (دانلود یک‌جای کل کتاب در scope این نسخه نیست — دقیقاً همون محدودیتی که Project Gutenberg با فایل EPUB/TXT کامل می‌تونه پوشش بده، بخش ۶ جدول بالا رو ببینید).

**مدل داده:** بدون فیلد جدید — همون پنج فیلد nullable که برای Vikidia اضافه شد (`sourceUrl`, `sourceName`, `author`, `license`, `publishedAt`) کافیه، بدون migration جدید. `sourceName = "Wikisource"`, `license = "CC BY-SA 4.0"`, `sourceUrl = "https://fr.wikisource.org/wiki/<عنوان URL-encode شده>"`. `author` در v1 مثل Vikidia می‌تونه `null` بمونه (استخراج دقیق نویسنده از `.headertemplate-author` اختیاریه، نه ضروری برای MVP). `publishedAt` رو می‌شه مثل Vikidia از تاریخ آخرین ویرایش صفحه پر کرد، با این تذکر که این تاریخ *ویرایش رونویسی در ویکی*ه، نه تاریخ انتشار اصلی اثر ادبی — همون‌طور که در بخش نمایش منبع باید روشن باشه.

**یکپارچگی با معماری موجود:** پیاده‌سازی به‌شکل `WikisourceContentSource : ContentSource` (پکیج `content` یا زیرپکیج `content/wikisource`، هم‌سبک `content/news`)، اضافه‌شدن به همون لیست منابعی که شیت «پیدا کردن مطلب» موازی جست‌وجو می‌کنه (Vikidia + RFI + France Info + Wikisource)؛ شکست یک منبع نتیجه‌های بقیه رو خراب نمی‌کنه (الگوی موجود). چون jsoup از قبل برای RFI/France Info اضافه شده، نیازی به کتابخونهٔ جدید نیست.

**نکتهٔ کیفیت کد (اختیاری، نه مسدودکننده):** اضافه‌شدن Wikisource یعنی یه نسخهٔ چهارم از تابع کمکی `httpGet`/`httpGetHtml` (بعد از `VikidiaClient`، `NewsFetcher`، `NewsContentSource` که از قبل در بخش «باقی‌مانده برای بعد» بالا به‌عنوان بدهی فنی ثبت شده). با اضافه‌شدن این یکی، شاید دیگه وقتشه یکی‌شون کنیم به‌جای اضافه‌کردن نسخهٔ چهارم؛ ولی این تصمیم مسدودکنندهٔ شروع کار نیست.

## ۷. تنظیمات و شخصی‌سازی ظاهر

وضعیت: **طرح — آمادهٔ پیاده‌سازی.**

یه صفحهٔ «تنظیمات» جدید که سه مورد شخصی‌سازی ظاهر رو در اختیار کاربر بذاره: تم روشن/تیرهٔ کل اپ، رنگ پس‌زمینهٔ صفحهٔ خوانش (مستقل از تم کل اپ)، و اندازهٔ فونت متن خوانش. تغییرات فوری روی صفحات باز اعمال می‌شن، بدون نیاز به ری‌استارت اپ.

### وضعیت فعلی کد (بررسی شد)

- هیچ `Theme.kt`ای در پروژه وجود نداره؛ تنها theming موجود یه `MaterialTheme {}` خالی در `MainActivity.kt` هست (بدون آرگومان `colorScheme`)، که یعنی اپ همیشه روی `lightColorScheme()` پیش‌فرض قفله و به تنظیم روشن/تیرهٔ سیستم اصلاً واکنش نشون نمی‌ده.
- `TextsListScreen.kt`، `VocabListScreen.kt`، `DictionarySheet.kt`، `VocabReviewScreen.kt` همگی از `MaterialTheme.colorScheme`/`MaterialTheme.typography` استفاده می‌کنن (هیچ رنگ hardcode‌شده‌ای ندارن) — پس با اضافه‌شدن یه `ColorScheme` تیره، خودکار هماهنگ می‌شن.
- `ReadingScreen.kt` تنها استثناست: یه `ReadingPalette` ثابت (`Background`/`Ink`/`InkFaded`/`HighlightBg`/`HighlightInk`/`Divider`/`Accent`) با رنگ‌های hardcode (پس‌زمینهٔ کاغذی گرم) که در ~۲۵ جا استفاده شده.
- اندازه‌های فونت هم در `ReadingScreen.kt` پخش‌ان: پاراگراف/آیتم‌لیست `19.sp`/۲۹–۳۱sp، ترجمه ۱۳–۱۴sp، و تابع‌های سطح‌محور `headerFontSize(level)`/`headerLineHeight(level)`.
- الگوی ذخیرهٔ تنظیمات موجود (`VocabPrefs.kt`): یه `object` ساده با `SharedPreferences(name, MODE_PRIVATE)`، متدهای sync گرفتن/ست‌کردن با `Context` به‌عنوان پارامتر اول، بدون Flow/coroutine. مبنای `AppearancePrefs.kt` جدید همین الگوئه.
- ناوبری: یه `NavHost` واحد در `MainActivity.kt` با روت‌های `texts`/`reading/{textId}`/`vocab`/`vocab_review/{scope}`؛ روت `settings` جدید همین‌جا اضافه می‌شه.
- هیچ `CompositionLocal` سفارشی‌ای در پروژه نیست (فقط `LocalLayoutDirection` استاندارد برای LTR کردن متن فرانسوی استفاده شده).

### طراحی فنی

**۱. تم روشن/تیرهٔ کل اپ** — فایل جدید `ui/theme/Theme.kt`:

```kotlin
enum class ThemeMode { SYSTEM, LIGHT, DARK }

@Composable
fun FrenchReaderTheme(themeMode: ThemeMode, content: @Composable () -> Unit) {
    val darkTheme = when (themeMode) {
        ThemeMode.SYSTEM -> isSystemInDarkTheme()
        ThemeMode.LIGHT -> false
        ThemeMode.DARK -> true
    }
    val colorScheme = if (darkTheme) darkColorScheme() else lightColorScheme()
    MaterialTheme(colorScheme = colorScheme, content = content)
}
```

جایگزین `MaterialTheme {}` فعلی در `MainActivity.kt` می‌شه (بدون dynamic color؛ `minSdk 26` پایین‌تر از حداقل API 31 لازم برای `dynamicColorScheme` هست و اضافه‌کردنش نیاز به guard جدا داره که فعلاً لازم نیست).

**۲. رنگ پس‌زمینهٔ خوانش (مستقل از تم کل اپ)** — ۳ پالت ثابت، همون‌جا در `Theme.kt`:

```kotlin
enum class ReadingBackground { SEPIA, WHITE, DARK }

data class ReadingPalette(
    val background: Color, val ink: Color, val inkFaded: Color,
    val highlightBg: Color, val highlightInk: Color,
    val divider: Color, val accent: Color
)

fun readingPaletteFor(background: ReadingBackground): ReadingPalette = when (background) {
    ReadingBackground.SEPIA -> ReadingPalette(
        background = Color(0xFFFBF6EC), ink = Color(0xFF2E2A22), inkFaded = Color(0xFF6B6252),
        highlightBg = Color(0xFFF6D97A), highlightInk = Color(0xFF2E2A22),
        divider = Color(0xFFE6DDC8), accent = Color(0xFF8A6D3B)
    ) // مقادیر دقیقاً همون ReadingPalette فعلی -- پیش‌فرض، بدون تغییر ظاهری برای کسی که تنظیمات رو دست نمی‌زنه
    ReadingBackground.WHITE -> ReadingPalette(
        background = Color(0xFFFFFFFF), ink = Color(0xFF1A1A1A), inkFaded = Color(0xFF5C5C5C),
        highlightBg = Color(0xFFFFE082), highlightInk = Color(0xFF1A1A1A),
        divider = Color(0xFFE0E0E0), accent = Color(0xFF3B6EA8)
    )
    ReadingBackground.DARK -> ReadingPalette(
        background = Color(0xFF1A1A1A), ink = Color(0xFFE8E4DA), inkFaded = Color(0xFFA8A296),
        highlightBg = Color(0xFF4A3F1E), highlightInk = Color(0xFFF6D97A),
        divider = Color(0xFF3A3A3A), accent = Color(0xFFD8B978)
    )
}
```

`ReadingScreen.kt`'s `private object ReadingPalette` حذف می‌شه؛ همه‌جا که `ReadingPalette.X` صدا زده می‌شه به `palette.X` تبدیل می‌شه (`val palette = readingPaletteFor(...)` یک‌بار بالای composable). مستقل از تم کل اپ باقی می‌مونه — کاربر می‌تونه مثلاً تم اپ روشن باشه ولی پس‌زمینهٔ خوانش تیره انتخاب کنه.

**۳. اندازهٔ فونت خوانش** — ۴ ضریب ثابت، همون‌جا:

```kotlin
enum class FontScale(val multiplier: Float, val label: String) {
    SMALL(0.85f, "کوچک"), MEDIUM(1f, "متوسط"), LARGE(1.15f, "بزرگ"), XLARGE(1.3f, "خیلی‌بزرگ")
}
```

هر `fontSize`/`lineHeight` هاردکد در `ReadingScreen.kt` (پاراگراف، آیتم‌لیست، ترجمه، `headerFontSize`/`headerLineHeight`) در `fontScale.multiplier` ضرب می‌شه.

**۴. ذخیره‌سازی** — `data/AppearancePrefs.kt` جدید، دقیقاً با الگوی `VocabPrefs.kt`: سه جفت getter/setter (`getThemeMode`/`setThemeMode`, `getReadingBackground`/`setReadingBackground`, `getFontScale`/`setFontScale`)، هر کدوم یه `String` (نام enum) روی `SharedPreferences` ذخیره می‌کنن، پیش‌فرض‌ها `SYSTEM`/`SEPIA`/`MEDIUM`.

**۵. پخش زنده‌ی تنظیمات بدون ری‌استارت** — چون `AppearancePrefs` sync و بدون Flow هست، یه state holder سبک در سطح اپ لازمه که هم `SettingsScreen` (موقع تغییر) و هم `MainActivity`/`ReadingScreen` (برای خوندن) بهش ارجاع بدن:

```kotlin
object AppearanceState {
    var themeMode by mutableStateOf(ThemeMode.SYSTEM)
    var readingBackground by mutableStateOf(ReadingBackground.SEPIA)
    var fontScale by mutableStateOf(FontScale.MEDIUM)
}
```

مقداردهی اولیه از `AppearancePrefs` در `MainActivity.onCreate`؛ `SettingsScreen` هم‌زمان `AppearancePrefs.setX(...)` (ماندگاری) و `AppearanceState.x = ...` (بازتاب فوری در UI) رو صدا می‌زنه. بدون DI/ViewModel اضافه -- منطبق با سبک ساده و بدون‌فریم‌ورک بقیهٔ پروژه.

**۶. صفحهٔ تنظیمات و ناوبری** — `ui/SettingsScreen.kt` جدید (Scaffold+TopAppBar+Column، مثل بقیهٔ صفحات)، سه بخش رادیویی (تم/پس‌زمینه/فونت). روت `"settings"` به `AppNavHost` در `MainActivity.kt` اضافه می‌شه؛ یه آیکون تنظیمات جدید در نوار بالای `TextsListScreen.kt` (کنار آیکون‌های موجود) بازش می‌کنه.

### فایل‌های تغییریافته/جدید

- ایجاد: `ui/theme/Theme.kt` (`FrenchReaderTheme`, `ThemeMode`, `ReadingBackground`, `ReadingPalette` + `readingPaletteFor`, `FontScale`, `AppearanceState`)
- ایجاد: `data/AppearancePrefs.kt`
- ایجاد: `ui/SettingsScreen.kt`
- تغییر: `MainActivity.kt` (تم جدید + روت `settings`)
- تغییر: `ReadingScreen.kt` (حذف `ReadingPalette` قدیمی، استفاده از `palette`/`fontScale.multiplier` در همهٔ ~۲۵ + ~۱۰ جای قبلی)
- تغییر: `TextsListScreen.kt` (آیکون ورود به تنظیمات)

## ۸. سازمان‌دهی کتابخانه با پوشه و تگ

وضعیت: **ایده/طرح اولیه — هنوز اجرا نشده.**

هدف: متن‌های دانلودشده، واردشده از فایل یا Share، و متن‌هایی که کاربر دستی اضافه کرده بتونن داخل Library به شکل منظم دسته‌بندی بشن. در نسخهٔ اول، هر متن حداکثر داخل **یک پوشه** قرار می‌گیره، ولی می‌تونه هم‌زمان **چند تگ** داشته باشه؛ مثلاً پوشهٔ «آمادگی TCF» و تگ‌های «B2»، «اخبار» و «واژگان اقتصادی».

### تجربهٔ کاربری پیشنهادی

- ساخت، تغییر نام و حذف پوشه‌ها و تگ‌ها از صفحهٔ Library.
- انتخاب پوشه و تگ‌ها هنگام افزودن متن جدید یا از طریق ویرایش اطلاعات یک متن موجود.
- نمایش پوشه و تگ‌های هر متن روی ردیف یا صفحهٔ جزئیاتش، بدون شلوغ کردن نمای اصلی.
- فیلتر Library براساس یک پوشه و یک یا چند تگ؛ جست‌وجو و مرتب‌سازی فعلی در کنار این فیلترها حفظ می‌شن.
- متن‌هایی که هنوز دسته‌بندی نشدن در نمای «بدون پوشه» قابل دسترسی می‌مونن.
- حذف یک پوشه یا تگ هیچ متنی رو حذف نمی‌کنه؛ فقط ارتباط دسته‌بندی از متن‌ها برداشته می‌شه.

### طراحی فنی اولیه

- جدول `Folder` برای پوشه‌ها (`id`, `name`, `createdAt`) و یک فیلد nullable مثل `folderId` روی `TextDocument`؛ رابطهٔ متن و پوشه یک‌به‌چند است.
- جدول `Tag` برای نام تگ‌ها (`id`, `name`) و جدول واسط `TextTagCrossRef` برای رابطهٔ چندبه‌چند متن‌ها و تگ‌ها.
- نام پوشه و تگ بعد از حذف فاصله‌های ابتدا/انتها نباید خالی باشه؛ نام‌های تکراری بدون توجه به بزرگی/کوچکی حروف ساخته نشن.
- کوئری Library طوری توسعه پیدا کنه که ترکیب جست‌وجوی متنی، مرتب‌سازی، پوشه و تگ‌ها رو پشتیبانی کنه.
- migration دیتابیس باید متن‌های موجود رو بدون پوشه و بدون تگ نگه داره تا هیچ داده‌ای از بین نره.

### خارج از محدودهٔ نسخهٔ اول

- پوشه‌های تودرتو، رنگ یا آیکون اختصاصی پوشه‌ها و تگ‌ها، و پیشنهاد خودکار تگ با هوش مصنوعی.
- همگام‌سازی دسته‌بندی‌ها بین چند دستگاه.

## ۹. قابلیت‌های هوش مصنوعی محلی و رایگان

وضعیت: **نقشهٔ راه مرحله‌ای — هنوز اجرا نشده.**

هدف: قابلیت‌های هوشمند برنامه تا جای ممکن کاملاً روی گوشی اجرا بشن، متن و صدای کاربر به سرور فرستاده نشه، و استفاده از امکانات اصلی نیاز به اشتراک یا API پولی نداشته باشه. به‌جای وابسته‌کردن برنامه به یک مدل بزرگ، هر کار با کوچک‌ترین مدل تخصصی مناسب انجام می‌شه و LLM فقط برای کارهای مولد استفاده می‌شه.

### معماری مشترک مدل‌ها

- مدل‌های حجیم داخل APK قرار نمی‌گیرن؛ کاربر هر مدل رو هنگام فعال‌کردن قابلیت مربوط دانلود می‌کنه و می‌تونه بعداً حذفش کنه.
- یک صفحهٔ «مدل‌های آفلاین» حجم دانلود، فضای اشغال‌شده، وضعیت آماده‌سازی و دکمهٔ حذف هر مدل رو نمایش می‌ده.
- اجرای مدل‌ها پشت interfaceهای مستقل مثل `TranslationEngine`، `EmbeddingEngine`، `SpeechRecognitionEngine` و `LocalLlmEngine` قرار می‌گیره تا مدل یا runtime بعداً بدون بازنویسی UI قابل تعویض باشه.
- دانلود باید قابل ادامه، دارای checksum و فقط با اقدام آگاهانهٔ کاربر باشه؛ کمبود فضا یا RAM نباید باعث crash بشه.
- قابلیت‌های غیرهوشمند فعلی بدون دانلود هیچ مدلی و دقیقاً مثل قبل کار می‌کنن.
- نصب مدل‌ها کاملاً اختیاریه و هیچ دانلود خودکاری انجام نمی‌شه. نبودن یا حذف یک مدل فقط دکمه‌های وابسته به همون مدل رو غیرفعال می‌کنه، نه کل برنامه یا سایر قابلیت‌های هوشمند مستقل رو.
- قبل از دانلود، حجم فایل، فضای آزاد لازم و RAM پیشنهادی نمایش داده می‌شه. تشخیص سخت‌افزار فقط برای پیشنهاد و هشدار استفاده می‌شه، نه مسدودکردن اجباری کاربر.
- موتور هر مدل به‌صورت lazy فقط هنگام استفاده وارد RAM می‌شه، هم‌زمان فقط یک inference سنگین اجرا می‌شه، و در پایان کار/رفتن برنامه به پس‌زمینه یا دریافت `onTrimMemory` منابعش آزاد می‌شن.
- شکست بارگذاری یا توقف مدل به‌دلیل کمبود حافظه با پیام قابل‌فهم مدیریت می‌شه و کاربر بدون از دست‌دادن متن به حالت عادی برمی‌گرده.

### اولویت ۱: ورود متن از عکس با OCR محلی

- انتخاب عکس یا گرفتن عکس از صفحهٔ کتاب/مجله، استخراج متن فرانسوی، نمایش پیش‌نمایش قابل‌ویرایش و سپس ذخیره در Library.
- استفاده از **ML Kit Text Recognition v2** با مدل bundled لاتین؛ پردازش روی دستگاه و بدون ارسال تصویر به سرور.
- حفظ پاراگراف‌ها تا حد ممکن و نمایش هشدار برای بخش‌هایی که confidence پایینی دارن.
- نسخهٔ اول فقط متن فرانسوی چاپی با خط لاتین رو هدف می‌گیره؛ تشخیص دست‌خط و حفظ دقیق صفحه‌آرایی خارج از محدوده است.

### اولویت ۲: ترجمهٔ آفلاین فرانسوی ↔ فارسی

- افزودن حالت «ترجمهٔ سریع آفلاین» برای واژه، جمله یا پاراگراف انتخاب‌شده با **ML Kit On-device Translation**.
- بسته‌های فرانسوی و فارسی فقط در صورت درخواست دانلود می‌شن و از صفحهٔ مدیریت مدل‌ها قابل حذف هستن.
- ترجمهٔ خروجی به‌عنوان ترجمهٔ ماشینی نمایش داده می‌شه؛ این مدل برای ترجمهٔ ساده و غیررسمی مناسبه، نه ترجمهٔ ادبی یا توضیح دقیق ظرافت‌های زبانی.
- مسیر ترجمهٔ فعلی/دیکشنری حفظ می‌شه و ترجمهٔ آفلاین یک گزینهٔ مکمل خواهد بود.

### اولویت ۳: تگ‌گذاری و جست‌وجوی مفهومی

- استفاده از مدل embedding چندزبانهٔ **`paraphrase-multilingual-MiniLM-L12-v2`** (مجوز Apache 2.0، بردار ۳۸۴بعدی) از طریق ONNX Runtime یا LiteRT.
- ساخت embedding محلی برای عنوان و متن و استفاده از اون برای پیشنهاد تگ، پیدا کردن متن‌های مشابه و جست‌وجوی معنایی.
- اتصال مستقیم به پوشه/تگ بخش ۸: برنامه تگ‌ها رو فقط **پیشنهاد** می‌ده و ذخیرهٔ اون‌ها نیاز به تأیید کاربر داره.
- embedding متن‌ها هنگام شارژ بودن/درخواست کاربر به‌صورت دسته‌ای ساخته می‌شه و با تغییر متن دوباره محاسبه می‌شه.
- نسخهٔ اول از فهرست تگ‌های موجود پیشنهاد می‌ده؛ ساخت خودکار نام‌های جدید و دسته‌بندی کاملاً خودکار برای مرحلهٔ بعد می‌مونه.

### اولویت ۴: Shadowing و تشخیص گفتار آفلاین

- چرخهٔ تمرین: پخش یک جمله، ضبط تکرار کاربر، تبدیل صدای کاربر به متن، و مشخص‌کردن کلمات درست، حذف‌شده، اضافه‌شده یا متفاوت.
- استفاده از **`whisper.cpp`** با مدل multilingual `tiny` یا `base`؛ مدل به انتخاب کاربر دانلود می‌شه (`tiny` سریع‌تر، `base` دقیق‌تر).
- صدای ضبط‌شده به‌طور پیش‌فرض فقط روی دستگاه می‌مونه و کاربر می‌تونه صدای خودش و صدای اصلی رو پشت‌سرهم مقایسه کنه.
- نسخهٔ اول نمرهٔ علمی واج‌به‌واج نمی‌ده؛ Whisper مدل تشخیص گفتاره و confidence اون نباید به‌عنوان امتیاز قطعی تلفظ نمایش داده بشه.

### اولویت ۵: دستیار مطالعه با LLM محلی

- قابلیت‌ها: خلاصهٔ متن، توضیح جمله یا نکتهٔ گرامری، ساده‌سازی تقریبی به سطح A2/B1/B2، استخراج واژه‌های دشوار، ساخت سؤال درک مطلب و تولید فلش‌کارت پیشنهادی.
- مدل شروع پیشنهادی: **Qwen3 0.6B Instruct** با quantization چهار بیتی و مجوز Apache 2.0؛ گزینهٔ باکیفیت‌تر برای گوشی‌های قوی‌تر: Qwen3 1.7B یا Gemma 3 1B IT.
- پروفایل سبک پیشنهادی **Qwen3 0.6B Q4_K_M** است: فایل مدل حدود ۴۶۲ MiB و مصرف تقریبی RAM هنگام inference کوتاه حدود ۰٫۸ تا ۱٫۳ GiB؛ برای دانلود امن حداقل حدود ۱ GiB فضای آزاد پیشنهاد می‌شه. دستگاه ۴ GiB ممکنه با context کوتاه اجراش کنه، ولی ۶ GiB RAM یا بیشتر هدف مطمئن‌تریه.
- پروفایل اختیاری «کیفیت بالاتر» **Qwen3 1.7B Q4_K_M** است: فایل مدل حدود ۱٫۲۸ GB و مصرف تقریبی RAM حدود ۱٫۸ تا ۲٫۸ GiB؛ فقط برای دستگاه‌های قوی‌تر (ترجیحاً ۸ GiB RAM یا بیشتر) پیشنهاد می‌شه. این اعداد تخمینی و وابسته به runtime، context و شتاب‌دهندهٔ دستگاه هستن و باید در spike روی گوشی واقعی اندازه‌گیری بشن.
- runtime اولیه پس از spike عملکرد روی دستگاه هدف بین **LiteRT-LM** (API کاتلین و شتاب‌دهی Android) و **llama.cpp** (انعطاف بیشتر و مدل‌های GGUF) انتخاب می‌شه.
- نقطهٔ شروع spike، **Qwen3 0.6B Q4_K_M روی llama.cpp** است، چون پشتیبانی GGUF و کنترل مستقیم `n_ctx`، تعداد thread، mmap و چرخهٔ load/unload داره؛ این انتخاب موقت است و فقط بعد از نتیجهٔ benchmark نهایی می‌شه. LiteRT-LM خصوصاً در صورت انتخاب مدل‌های Gemma همچنان گزینهٔ مقایسه باقی می‌مونه.
- خروجی مدل همیشه با برچسب «پیشنهاد هوش مصنوعی» نمایش داده می‌شه و قبل از افزودن تگ، فلش‌کارت یا تغییر متن نیاز به تأیید کاربر داره.
- برای کاهش hallucination، دستیار فقط متن بازشده و اطلاعات ساختاریافتهٔ موجود رو در prompt دریافت می‌کنه؛ پاسخ‌هایی که باید قطعی باشن (مثل جنسیت اسم یا صرف فعل) در صورت امکان از دیکشنری میان، نه صرفاً LLM.
- مدل در اولین نسخه یک متن/بخش محدود رو با context پیش‌فرض ۲۰۴۸ توکن (حداکثر اولیهٔ ۴۰۹۶) و خروجی محدود پردازش می‌کنه تا RAM و زمان پاسخ کنترل بشه؛ context بسیار بزرگ، چت عمومی و agent متصل به اینترنت خارج از محدوده است.

وضعیت: **نسخهٔ اول (خلاصه‌سازی، توضیح گرامر، ساده‌سازی، استخراج واژه) پیاده‌سازی شده روی شاخهٔ `local-llm-assistant`** — Qwen3 0.6B Q4_K_M روی llama.cpp، طبق spike مستندشده در `docs/superpowers/spikes/llm-assistant-spike/`. سؤال درک مطلب و فلش‌کارت پیشنهادی هنوز در این نسخه نیستن. بررسی نهایی کل شاخه (`docs/superpowers/plans/2026-09-17-local-llm-reading-assistant.md` + `.superpowers/sdd/.../final-review.md`) دو مشکل Critical رو پیدا کرد که پیش از merge برطرف می‌شن (تک‌نمونه‌بودن engine در کل برنامه، و مدیریت exception در مرز JNI). موارد زیر Important/Minor هستن، عمداً برای بعد از این branch باقی می‌مونن (تصمیم کاربر، برای صرفه‌جویی در وقت و چون بازطراحی رابط کاربری در پیشه):

- **تکمیل تجربهٔ تأیید واژه‌های پیشنهادی:** ردیف واژهٔ پیشنهادی هوش مصنوعی فعلاً فقط دکمهٔ «Accepter» ساده داره و مستقیم با آخرین پوشهٔ استفاده‌شده ذخیره می‌کنه؛ باید مثل دیکشنری موجود، تعریف قابل‌ویرایش و انتخاب/تأیید پوشه (با اعتبارسنجی این‌که پوشه هنوز وجود داره) اضافه بشه.
- **مدیریت وضعیت دانلود مدل:** لغو دانلود، فایل‌های `.part` باقی‌مانده بعد از خطا/قطع برنامه، عدم تطابق احتمالی بین فلگ SharedPreferences و وجود واقعی فایل مدل، و دکمهٔ حذف مدل هنوز پیاده‌سازی نشدن.
- **قلاب چرخهٔ حیات برنامه:** `onTrimMemory`/رفتن برنامه به پس‌زمینه هنوز باعث آزادسازی فوری مدل نمی‌شن (فقط تایمر بی‌کاری ۲ دقیقه‌ای و بسته‌شدن شیت این کار رو می‌کنن)؛ و `LlmResult.Failure.TIMEOUT` تعریف شده ولی هیچ‌جا واقعاً استفاده/تولید نمی‌شه (بدون مکانیزم لغو واقعی برای تولید طولانی/گیرکرده).
- **موارد جزئی:** استخراج واژگان با نتیجهٔ خالی نه پیام موفقیت نه خطا نشون می‌ده؛ متن‌های صفحهٔ دانلود مدل فقط فرانسوی hardcode شدن (باید از resource سه‌زبانه بیان).

### اولویت ۶: صدای آفلاین (TTS)

- افزودن **sherpa-onnx** با مدل فرانسوی Piper/VITS به‌عنوان موتور آفلاین اختیاری.
- `edge-tts` برای کیفیت بالاتر آنلاین حفظ می‌شه و کاربر می‌تونه بین «کیفیت آنلاین» و «کاملاً آفلاین» انتخاب کنه.
- قبل از انتخاب مدل نهایی، کیفیت تلفظ فرانسوی، سرعت تولید، حجم مدل و سازگاری با متن‌های طولانی روی دستگاه هدف مقایسه می‌شن.

### ترتیب اجرا و معیار عبور هر مرحله

1. OCR محلی: استخراج و ورود موفق چند صفحهٔ واقعی فرانسوی با امکان اصلاح پیش از ذخیره.
2. ترجمهٔ آفلاین: ترجمهٔ فرانسوی–فارسی بدون شبکه و مدیریت صحیح دانلود/حذف بسته‌ها.
3. embedding: جست‌وجوی مفهومی و پیشنهاد تگ با زمان پاسخ قابل‌قبول روی کل Library.
4. Shadowing: تشخیص آفلاین جمله و مقایسهٔ کلمه‌به‌کلمه بدون ادعای نمرهٔ تلفظ دقیق.
5. LLM محلی: spike مقایسه‌ای روی مجموعه‌ای ثابت از متن‌ها و promptها؛ انتخاب مدل فقط بعد از سنجش کیفیت فرانسوی، سرعت، RAM و مصرف باتری.
6. TTS آفلاین: انتخاب مدل بعد از مقایسهٔ شنیداری با صدای آنلاین فعلی.

هر مرحله مستقل تحویل داده می‌شه؛ اضافه‌شدن LLM نباید پیش‌نیاز OCR، ترجمه، جست‌وجوی مفهومی یا تمرین گفتار باشه.

### پروتکل spike دستیار محلی

چون benchmark استاندارد و قابل‌مقایسه‌ای برای Qwen3 0.6B روی گوشی‌های Android با ۴/۶/۸ GiB RAM وجود نداره، اعداد منابع بیرونی فقط تخمین اولیه محسوب می‌شن و مبنای تأیید قابلیت نیستن. آزمایش باید روی حداقل دو یا سه **گوشی واقعی** از رده‌های ضعیف/متوسط/قوی انجام بشه؛ emulator برای نتیجه‌گیری دربارهٔ سرعت، RAM، باتری، GPU یا حرارت معتبر نیست.

برای قابل‌تکرار بودن، یک مجموعهٔ ثابت از متن‌ها و promptهای فرانسوی پروژه نگه‌داری می‌شه و هر ترکیب مدل/runtime با تنظیمات یکسان (از جمله prefill حدود ۵۱۲ توکن، خروجی حدود ۱۲۸ توکن، contextهای ۲۰۴۸ و ۴۰۹۶، و چند مقدار محدود thread) اجرا می‌شه. اجرای اولیه می‌تونه با `llama-bench` و یک harness کوچک headless روی دستگاه انجام بشه؛ ادغام UI فقط بعد از عبور این مرحله شروع می‌شه.

برای هر اجرا این موارد ثبت می‌شن:

- اندازهٔ واقعی فایل مدل و فضای موقت لازم برای دانلود امن.
- زمان load، زمان اولین توکن، سرعت prefill و سرعت decode (token/s).
- peak RSS/PSS فرایند با ابزارهای Android مثل `adb shell dumpsys meminfo`؛ حجم فایل مدل به‌تنهایی معیار مصرف RAM نیست.
- دمای دستگاه، افت سرعت در اجرای تکراری، مصرف باتری در یک سناریوی ثابت و زمان آزادشدن حافظه بعد از unload/`onTrimMemory`.
- موفقیت یا شکست روی contextهای ۲K/۴K، رفتار در کمبود حافظه و بازگشت سالم به حالت بدون LLM.
- کیفیت فرانسوی با rubric ثابت برای خلاصه، توضیح گرامر، ساده‌سازی CEFR، سؤال درک مطلب و JSON ساختاریافته؛ پاسخ نادرست یا ساختگی هم شمارش می‌شه.

معیار انتخاب نهایی فقط بیشترین token/s نیست؛ مدل/runtime منتخب باید بدون crash و فشار غیرقابل‌قبول حافظه، کیفیت فرانسوی قابل‌استفاده و زمان پاسخ مناسب برای درخواست‌های کوتاه داشته باشه. ادعاهای عملکردی مدل‌های دیگر (مثلاً مصرف باتری Gemma) به Qwen تعمیم داده نمی‌شن و مقدار RAM دستگاه هم به‌تنهایی تعیین نمی‌کنه CPU، GPU یا NPU قابل استفاده است؛ این موضوع به SoC، driver و پشتیبانی runtime وابسته است.

---
*این سند فقط یک برنامهٔ ثبت‌شده است.*
