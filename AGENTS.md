# Project rules

- Codex uses LOW reasoning effort. Codex uses default (medium) effort (low pin lifted by the user 2026-09-21); standard service tier only.
- Write implementation code via `codex exec` when practical; verify diffs and build yourself.
- Reply in English.
