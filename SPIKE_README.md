# آزمون فنی: TTS فرانسوی روی اندروید (Chaquopy + edge-tts)

> **بروزرسانی:** فایل‌های Gradle از Kotlin DSL (`.kts`) به Groovy (`.gradle`)
> تغییر کردند، چون افزونهٔ Chaquopy بلاک `python { pip { ... } } }` را داخل
> `defaultConfig` در Kotlin DSL نمی‌شناسد (خطای «Unresolved reference:
> python»). این یک محدودیت شناخته‌شدهٔ خود Chaquopy است، نه ایراد پروژه؛ در
> Groovy این مشکل وجود ندارد. اگر پوشهٔ پروژه را از قبل باز کرده‌اید، آن را
> ببندید و بعد از دریافت فایل‌های جدید دوباره در Android Studio باز کنید تا
> `settings.gradle`/`build.gradle` جدید (به‌جای نسخهٔ `.kts`) خوانده شوند.

این پوشه یک پروژهٔ کوچک Android Studio است، **نه اپ نهایی**. هدفش فقط اثبات یا رد
همان مسیر پرریسکی است که طرح اصلی (`french-reader-design.md`) به‌عنوان
«اولین آزمون فنی» مشخص کرده بود: آیا می‌توان edge-tts را از طریق Chaquopy در
اندروید اجرا کرد و از خروجی آن (فایل MP3 + رویدادهای زمان‌بندی کلمه) برای
همگام‌سازی متن با صدا استفاده کرد؟

## چرا این کار را نمی‌توانستم از سمت من کامل تست کنم

محیط ابری من دو محدودیت شبکه دارد که روی گوشی شما وجود ندارد:

1. دسترسی به `dl.google.com` و مخزن Maven مربوط به Chaquopy (`chaquo.com/maven`)
   مسدود است، پس نمی‌توانم SDK اندروید یا پلاگین Chaquopy را دانلود کنم و
   پروژه را این‌جا کامپایل کنم.
2. حتی فراخوانی مستقیم edge-tts هم از این محیط با خطای ۴۰۳ روی هندشیک
   وب‌سوکت مواجه شد (به‌احتمال زیاد به‌خاطر پراکسی امنیتی محیط، نه ایراد
   کد) — درحالی‌که همین منطق در پروژهٔ خودتان (`ankideck`) با اینترنت عادی
   قبلاً کار کرده است.

پس کد اینجا **نوشته و از نظر منطقی بازبینی شده**، ولی هنوز **روی دستگاه واقعی
اجرا نشده**. آزمون واقعی را شما باید روی مک و گوشی‌تان انجام دهید و نتیجه
(یا خطا/لاگ) را برایم بفرستید.

## ریسک اصلی که این آزمون باید روشن کند

`edge-tts` به `aiohttp` وابسته است و `aiohttp` خودش به چند پکیج با کد C
وابسته است: `multidict`, `yarl`, `frozenlist`, `propcache`. Chaquopy فقط
پکیج‌هایی را نصب می‌کند که برای معماری‌های اندروید (arm64-v8a و...) از قبل
کامپایل شده باشند. اگر Gradle sync روی `pip { install("edge-tts") }` گیر
کند یا خطای «no matching distribution» بدهد، یعنی این مسیر معماری نیاز به
بازبینی دارد (طبق پیش‌بینی خود طرح اصلی) — راه‌حل‌های جایگزین در پایین همین
فایل آمده.

## پیش‌نیاز روی مک شما

به نظر می‌رسد Android Studio / Android SDK از قبل روی مک شما نصب است (پوشهٔ
`~/.android` دیدم). اگر نه، Android Studio را از
`https://developer.android.com/studio` نصب کنید.

## مراحل build و اجرا

1. Android Studio را باز کنید → Open → پوشهٔ `FrenchReaderSpike` را انتخاب
   کنید (همین پوشه‌ای که این فایل در آن است).
2. صبر کنید Gradle sync کامل شود. **این مرحله همان آزمون است** — اگر خطایی
   در نصب پکیج‌های پایتون (`pip install`) داد، همان‌جا screenshot بگیرید و
   برایم بفرستید.
3. گوشی اندرویدتان را با کابل USB وصل کنید، «USB debugging» را در
   Developer Options فعال کنید و اجازهٔ اتصال را روی گوشی تأیید کنید.
4. دکمهٔ Run ▶ را در Android Studio بزنید و گوشی را به‌عنوان مقصد انتخاب
   کنید (یا از ترمینال: `./gradlew installDebug` سپس اپ را دستی از روی
   گوشی باز کنید).
5. در اپ، متن آزمایشی از پیش پرشده (شامل تکیه‌کلام‌ها، آپاستروف، حروف
   دارای اکسان، گیومهٔ فرانسوی، خط تیرهٔ بلند و یک جملهٔ تکراری) را همان‌طور
   نگه دارید و دکمهٔ «Generate & Play» را بزنید.

## چه‌چیزی را باید بررسی/گزارش کنید

- آیا Gradle sync بدون خطا تمام شد؟ (مهم‌ترین نتیجه)
- آیا صدا واقعاً پخش شد؟
- آیا فهرست «word boundaries» زیر دکمه پر شد و کلمهٔ در حال پخش هایلایت
  می‌شود؟
- آیا کلمات دارای اکسان/آپاستروف درست در فهرست ظاهر شدند یا بهم‌ریخته‌اند؟
- اگر خطا داد: پیام کامل خطا (از Logcat در Android Studio، فیلتر روی
  `AndroidRuntime` یا `System.err`) را برایم بفرستید.

## اگر pip install edge-tts در Chaquopy شکست خورد

راه‌های جایگزین به ترتیب اولویت:

1. نسخهٔ قدیمی‌تر `edge-tts` را امتحان کنید که وابستگی‌های سبک‌تری دارد:
   `pip { install("edge-tts==6.1.9") }` (یا نسخه‌ای که راهنمای Chaquopy
   (`https://chaquo.com/chaquopy/doc/current/versions.html`) پشتیبانی
   می‌کند را چک کنید).
2. اگر `aiohttp` قابل نصب نبود، می‌توان به‌جای آن یک نسخهٔ کوچک‌شدهٔ
   دست‌ساز از پروتکل edge-tts نوشت که فقط با `requests`/`http.client` روی
   HTTP (نه وب‌سوکت) کار کند — پیچیده‌تر است ولی وابستگی C ندارد.
3. اگر Chaquopy به‌طور کلی مشکل‌ساز بود، معماری جایگزین: یک سرویس کوچک
   Python (روی سرور شخصی/Termux) که فقط MP3+زمان‌بندی تولید کند و اپ
   اندروید فقط HTTP بزند — این دقیقاً همان «بازبینی معماری» است که طرح
   اصلی از قبل به‌عنوان fallback در نظر گرفته.

## بعد از این آزمون

اگر نتیجه مثبت بود، طبق طرح می‌رویم سراغ ساخت اپ کامل: صفحهٔ متن‌ها، صفحهٔ
مطالعه با هایلایت جمله، دیکشنری WordReference در WebView، فهرست لغات
ذخیره‌شده، پایگاه دادهٔ محلی (Room)، پشتیبان‌گیری JSON، حالت شب، و سرویس
پخش پس‌زمینه با کنترل از اعلان.
